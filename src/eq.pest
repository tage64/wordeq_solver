variables = { "Variables {" ~ ASCII_ALPHA_UPPER* ~ "}" }
terminals = { "Terminals {" ~ ASCII_ALPHA_LOWER* ~ "}" }
equation = { "Equation: " ~ ASCII_ALPHA* ~ " = " ~ ASCII_ALPHA* }
file = {
    SOI ~
    variables ~ NEWLINE ~
    terminals ~ NEWLINE ~
    equation ~ NEWLINE ~
    "SatGlucose(100)" ~
    EOI
}
